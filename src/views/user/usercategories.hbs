<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Categories</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <div class="container d-flex justify-content-between mt-4">
        <div class="col-lg-2 col-md-4">
            <a href="/user" class="btn btn-primary btn-lg btn-block mb-3">Back</a>
        </div>
        <div class="col-lg-2 col-md-4">
            <a href="/user/logout" class="btn btn-primary btn-lg btn-block mb-3">Logout</a>
        </div>
    </div>
    <div class="container mt-5">
        <h1 class="mb-4">Vendors in Category: {{categorie}}</h1>
        <div class="row">
            {{#each vendordata}}
            <div class="col-md-4 mb-4">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">{{this.user}}</h5>
                        <p class="card-text">Email: {{this.email}}</p>
                        <p class="card-text">Mobile number: {{this.mobile}}</p>
                        <a href="/user/allItems/{{this._id}}" class="btn btn-primary">Shop Items</a>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>