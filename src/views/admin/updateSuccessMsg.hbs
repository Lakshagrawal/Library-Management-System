<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Success</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin-top: 50px;
        }

        h1 {
            color: green;
        }
    </style>
</head>

<body>
    <h1>Update Successful!</h1>

    {{#if error}}
        {{#if (eq error 'MissingFields')}}
            <p>Your user information has been updated successfully.</p>
            <a href="/admin/allUser">Back to Users</a>
        {{else if (eq error 'UsernameTaken')}}
            <p>Your Vendor information has been updated successfully.</p>
            <a href="/admin/allVendor">Back to Users</a>
        {{/if}}
    {{/if}}
    
</body>

</html>